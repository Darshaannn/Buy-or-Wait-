# Backend
HOST=0.0.0.0
PORT=8000
FRONTEND_ORIGIN=http://localhost:5173

# Optional AI evidence extraction
# (If not provided, the engine operates using local deterministic pattern extraction rules)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GROQ_API_KEY=
GROQ_MODEL=qwen/qwen3.8-27b

# Frontend — set this in Vercel, not Render
# VITE_API_BASE_URL=http://localhost:8000
